#ifndef LVGL_PORT_H
#define LVGL_PORT_H

/**
 * @brief Inicializa a biblioteca LVGL e a conecta ao driver do display.
 * Configura os buffers de desenho e o timer de tick.
 */
void lvgl_port_init();

#endif // LVGL_PORT_H